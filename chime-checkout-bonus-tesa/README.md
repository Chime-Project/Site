# Chime Health — 2-step checkout, COMBO edition: GLP-1 + BONUS FREE Tesamorelin (product → checkout)

`chime-checkout-bonus/` (GLP-1 + BONUS FREE NAD+) with the free gift swapped for **Tesamorelin** —
2026-09-09 request: "that NAD page, make a version swapped with Tesamorelin as the free gift".
Same layout, same emphasis (both products, one low price); only the gift changes. Generated by the
same script in MODE `bonus-tesa`:

    cd ~/Sites/chime && python3 uploads/inv-checkout-flow/build-chime-checkout.py bonus-tesa

(`bonus` rebuilds the NAD+ edition; the gift product is the `GIFT` table at the top of the build —
name, vial file, "why both" tile copy.) Edit the script and re-run rather than hand-editing the
compiled sheets.

## Preview

    cd ~/Sites/chime && python3 -m http.server 8791

then http://localhost:8791/chime-checkout-bonus-tesa/product.html → `checkout.html`.

## ⚠️ Stand-ins the client still has to confirm

- **The Tesamorelin value is "$299"** everywhere — carried over from the NAD+ edition
  (`BONUS_VALUE` in the build). No Tesamorelin value has been given; nothing on the site prices
  it. Change the one constant when the client sends the figure.
- **Prices are the 1 + 3 ladder, unchanged.** Semaglutide $299/mo monthly, $249/mo on 3 months
  + 1 free ($747 due today); Tirzepatide $359 / $299 ($897 due today). No combo price points.
- **Copy.** The "why both" tile says "Tesamorelin protects lean mass — peptide support for lean
  mass and metabolism while you lose" (the wording select-plan-v2.html already uses for it).
  Marketing copy in Chime voice, not a clinical statement — review with counsel.

## What differs from `chime-checkout-bonus/`

Every "NAD+" becomes "Tesamorelin": card titles ("Semaglutide + Tesamorelin Combo"), sub-lines,
the hero line and ribbon, the combo tile (`images/vial-tesamorelin.webp` from `uploads/vials/`,
365×800), the bonus bar, the why-both strip, the in-card callout, the "Your plan, all in" bonus
row, the sticky bar, the recommendation card, and on checkout the badge, the bonus line and the
value-stack row. Totals unchanged (the bonus is $0). `?v=20260933`.

Everything else is as documented in `chime-checkout-bonus/README.md` and `chime-checkout/README.md`.
